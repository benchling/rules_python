load("@rules_python//python:defs.bzl", "py_library")

# "conftest" is the name Gazelle generates for conftest.py, so this target is
# not adopted.
py_library(
    name = "conftest",
    srcs = [
        "bar.py",
        "baz.py",
    ],
    tags = ["keep_me"],
)
